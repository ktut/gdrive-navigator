# Google OAuth Configuration
# Get these values from Google Cloud Console: https://console.cloud.google.com/
# 
# 1. Create a new project (or use existing)
# 2. Enable Google Drive API
# 3. Go to APIs & Services > Credentials
# 4. Create OAuth 2.0 Client ID (Web application)
# 5. Add http://localhost:5173 to Authorized JavaScript origins
# 6. Copy the Client ID and paste it below

VITE_GOOGLE_CLIENT_ID=your-client-id-here.apps.googleusercontent.com

# Optional: Other environment variables (not currently used but defined in types)
VITE_API_KEY=
VITE_GOOGLE_CLIENT_SECRET=
VITE_GOOGLE_REDIRECT_URI=
VITE_FRONTEND_URL=
